<section id="booking">
	<h2>Book a Consultation</h2>
	<p class="section-subtitle">
		Let's create your perfect minimalist tattoo
	</p>
	<div class="booking-container">
		<div class="typeform-placeholder">
			<div class="placeholder-content">
				<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
				</svg>
				<h3>Typeform Integration</h3>
				<p>Replace this placeholder with your Typeform embed code</p>
				<div class="instructions">
					<p><strong>To integrate your Typeform:</strong></p>
					<ol>
						<li>Create your booking form on <a href="https://www.typeform.com" target="_blank">Typeform.com</a></li>
						<li>Get the embed code from your form settings</li>
						<li>Replace the content of this div with your embed code</li>
					</ol>
					<div class="code-example">
						<code>
							&lt;div data-tf-live="YOUR_FORM_ID"&gt;&lt;/div&gt;<br />
							&lt;script src="//embed.typeform.com/next/embed.js"&gt;&lt;/script&gt;
						</code>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	#booking {
		background: #f5f5f5;
	}

	.booking-container {
		max-width: 900px;
		margin: 0 auto;
		padding: 2rem;
	}

	.typeform-placeholder {
		background: #fff;
		border: 1px solid #e5e5e5;
		border-radius: 2px;
		padding: 4rem 3rem;
		text-align: center;
		min-height: 500px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.placeholder-content {
		max-width: 600px;
	}

	.placeholder-content svg {
		width: 60px;
		height: 60px;
		color: #1a1a1a;
		margin: 0 auto 2rem;
		opacity: 0.3;
	}

	.placeholder-content h3 {
		font-size: 1.5rem;
		margin-bottom: 1rem;
		color: #1a1a1a;
		font-weight: 500;
	}

	.placeholder-content > p {
		color: #999;
		font-size: 1rem;
		margin-bottom: 2.5rem;
	}

	.instructions {
		text-align: left;
		background: #fff;
		padding: 2rem;
		border-radius: 2px;
		border: 1px solid #e5e5e5;
		margin-top: 1rem;
	}

	.instructions p {
		margin-bottom: 1rem;
		color: #666;
		font-size: 0.95rem;
	}

	.instructions strong {
		color: #1a1a1a;
		font-size: 1rem;
		font-weight: 600;
	}

	.instructions ol {
		margin-left: 1.5rem;
		margin-bottom: 1.5rem;
		color: #777;
		line-height: 1.8;
		font-size: 0.95rem;
	}

	.instructions ol li {
		margin-bottom: 0.5rem;
	}

	.instructions a {
		color: #1a1a1a;
		text-decoration: underline;
		transition: opacity 0.3s;
	}

	.instructions a:hover {
		opacity: 0.6;
	}

	.code-example {
		background: #fafafa;
		padding: 1.5rem;
		border-radius: 2px;
		border: 1px solid #e5e5e5;
		overflow-x: auto;
		margin-top: 1rem;
	}

	.code-example code {
		font-family: 'Courier New', monospace;
		font-size: 0.85rem;
		color: #333;
		line-height: 1.6;
	}

	@media (max-width: 768px) {
		.booking-container {
			padding: 1rem;
		}

		.typeform-placeholder {
			padding: 2rem 1.5rem;
			min-height: auto;
		}

		.placeholder-content svg {
			width: 50px;
			height: 50px;
		}

		.placeholder-content h3 {
			font-size: 1.3rem;
		}

		.instructions {
			padding: 1.5rem;
		}
	}
</style>
