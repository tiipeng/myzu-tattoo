<section class="hero">
	<div class="hero-content">
		<p class="hero-label">Japanese Tattoo Studio</p>
		<h1>myzu</h1>
		<div class="divider"></div>
		<p class="hero-subtitle">
			Minimalist tattoo art inspired by Japanese aesthetics
		</p>
		<div class="hero-buttons">
			<a href="#booking" class="btn-primary">Book Consultation</a>
			<a href="#gallery" class="btn-secondary">View Work</a>
		</div>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		background: linear-gradient(180deg, #fafafa 0%, #f5f5f5 100%);
		position: relative;
		overflow: hidden;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 0;
		right: 0;
		width: 400px;
		height: 400px;
		background: radial-gradient(circle, rgba(26, 26, 26, 0.03) 0%, transparent 70%);
		border-radius: 50%;
	}

	.hero::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 0;
		width: 300px;
		height: 300px;
		background: radial-gradient(circle, rgba(26, 26, 26, 0.02) 0%, transparent 70%);
		border-radius: 50%;
	}

	.hero-content {
		position: relative;
		z-index: 2;
		text-align: center;
		padding: 2rem;
		animation: fadeInUp 1s ease-out 0.3s both;
	}

	.hero-label {
		font-size: 0.85rem;
		letter-spacing: 2px;
		color: #999;
		text-transform: uppercase;
		font-weight: 500;
		margin-bottom: 1.5rem;
	}

	h1 {
		font-family: 'Playfair Display', serif;
		font-size: 6rem;
		line-height: 1;
		margin-bottom: 1.5rem;
		letter-spacing: 8px;
		color: #1a1a1a;
		font-weight: 400;
	}

	.divider {
		width: 40px;
		height: 1px;
		background: #1a1a1a;
		margin: 2rem auto;
	}

	.hero-subtitle {
		font-size: 1.1rem;
		color: #666;
		max-width: 500px;
		margin: 0 auto 3rem;
		line-height: 1.8;
		font-weight: 300;
		letter-spacing: 0.5px;
	}

	.hero-buttons {
		display: flex;
		gap: 2rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn-primary,
	.btn-secondary {
		padding: 0.85rem 2.2rem;
		border-radius: 2px;
		font-weight: 500;
		text-decoration: none;
		transition: all 0.3s;
		font-size: 0.95rem;
		letter-spacing: 1px;
		text-transform: uppercase;
	}

	.btn-primary {
		background: #1a1a1a;
		color: #fafafa;
		border: 1px solid #1a1a1a;
	}

	.btn-primary:hover {
		opacity: 0.8;
	}

	.btn-secondary {
		background: transparent;
		color: #1a1a1a;
		border: 1px solid #1a1a1a;
	}

	.btn-secondary:hover {
		background: #1a1a1a;
		color: #fafafa;
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 4rem;
			letter-spacing: 4px;
		}

		.hero-subtitle {
			font-size: 1rem;
		}

		.hero-buttons {
			flex-direction: column;
			align-items: center;
			gap: 1rem;
		}

		.btn-primary,
		.btn-secondary {
			width: 100%;
			max-width: 280px;
		}
	}
</style>
