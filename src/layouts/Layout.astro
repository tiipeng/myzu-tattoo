<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
		<meta name="generator" content={Astro.generator} />
		<title>myzu - Tattoo Studio</title>
		<meta name="description" content="Minimalist tattoo studio">
	</head>
	<body>
		<nav>
			<div class="nav-container">
				<a href="#" class="logo">myzu</a>
				<ul class="nav-links">
					<li><a href="#about">About</a></li>
					<li><a href="#gallery">Gallery</a></li>
					<li><a href="#booking" class="nav-cta">Book</a></li>
				</ul>
			</div>
		</nav>
		<slot />
		<footer>
			<p>&copy; 2025 myzu. All rights reserved.</p>
		</footer>
	</body>
</html>

<style is:global>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
		background: #fafafa;
		color: #1a1a1a;
		line-height: 1.6;
	}

	nav {
		position: fixed;
		top: 0;
		width: 100%;
		background: rgba(250, 250, 250, 0.98);
		backdrop-filter: blur(10px);
		z-index: 1000;
		border-bottom: 1px solid #e5e5e5;
	}

	.nav-container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 1.25rem 2rem;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo {
		font-family: 'Playfair Display', serif;
		font-size: 1.75rem;
		letter-spacing: 3px;
		color: #1a1a1a;
		text-decoration: none;
		transition: opacity 0.3s;
		font-weight: 500;
	}

	.logo:hover {
		opacity: 0.7;
	}

	.nav-links {
		display: flex;
		list-style: none;
		gap: 2.5rem;
		align-items: center;
	}

	.nav-links a {
		color: #1a1a1a;
		text-decoration: none;
		font-weight: 400;
		font-size: 0.9rem;
		transition: opacity 0.3s;
		letter-spacing: 0.5px;
	}

	.nav-links a:hover {
		opacity: 0.6;
	}

	.nav-cta {
		background: #1a1a1a;
		padding: 0.6rem 1.4rem;
		border-radius: 2px;
		color: #fafafa !important;
		font-weight: 500;
		transition: all 0.3s;
		font-size: 0.9rem;
	}

	.nav-cta:hover {
		opacity: 0.8;
	}

	footer {
		background: #fff;
		padding: 2rem;
		text-align: center;
		border-top: 1px solid #e5e5e5;
		color: #999;
		font-size: 0.85rem;
	}

	section {
		padding: 5rem 2rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	h2 {
		font-family: 'Playfair Display', serif;
		font-size: 2.5rem;
		letter-spacing: 2px;
		margin-bottom: 1rem;
		text-align: center;
		color: #1a1a1a;
		font-weight: 500;
	}

	.section-subtitle {
		text-align: center;
		color: #777;
		max-width: 600px;
		margin: 0 auto 3rem;
		font-size: 1rem;
		font-weight: 300;
	}

	@media (max-width: 768px) {
		.nav-links {
			gap: 1.5rem;
		}

		.nav-links a {
			font-size: 0.85rem;
		}

		.nav-cta {
			padding: 0.5rem 1rem;
		}

		section {
			padding: 3rem 1.5rem;
		}

		h2 {
			font-size: 2.25rem;
		}
	}
</style>
